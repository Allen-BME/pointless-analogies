name: AWS Continuous Deployment

on:
  push:
    branches:
      - actions_cd
  pull-request:
    branches:
      - actions_cd

run-name: "Continuous Deployment: ${{ github.head_ref }} by @${{ github.actor }}"

jobs:
  test_cd:
    runs-on: ubuntu-latest
    steps:
      - name: Test CD
        run: echo "Test of Continuous Deployment"

# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Set up Python
#         uses: actions/setup-python@v4
#         with:
#           python-version: "3.11"
#       - name: Set up Node
#         uses: actions/setup-node@v3
#         with: 
#           node-version: "16"
#       - name: Install Python dependencies and CDK
#         run: |
#           python -m pip install --upgrade pip
#           pip install -r requirements
#           npm install -g aws-cdk
#       - name: Configure AWS credentials
#                 uses: aws-actions/configure-aws-credentials@master
#                 with:
#                   aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY }}
#                   aws-secret-access-key: ${{ secrets.AWS_SECRET_KEY }}
#                   aws-region: "us-east-1"
#       - name: Deploy to AWS
#           run: cdk deploy --app="python3 ${PWD}/app.py" --require-approval=never
#       - name: Destroy stack
#           run: cdk destroy --app="python3 ${PWD}/app.py" --force
